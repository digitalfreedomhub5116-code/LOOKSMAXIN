[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd web && npm install && npm run build && cd .."

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
